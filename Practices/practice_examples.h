#ifndef TEMPLATE_practice_examples_H
#define TEMPLATE_practice_examples_H

#include <map>
#include <string>
#include <vector>

/**
 *  Example class used for GTest demo
 */
  // // Unfold the code
  class practice_examples {
  std::vector<char> Unfold(const vector<char> &A, const vector<int> &R);
  private:
};

#endif